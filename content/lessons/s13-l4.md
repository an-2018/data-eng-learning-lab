# Handle late and corrected activity

## What you will be able to do

Process a second batch with a duplicate, a late event, and a correction, then reconcile the Gold attributes.

## The problem

You are working with workforce and project data that will be used by another team. Your result must have a clear meaning, survive incomplete records, and be reproducible. This lesson focuses on **handle late and corrected activity** within media audience intelligence pipelines.

## Build the mental model

Use event time, watermark, checkpoint, stable event IDs, and an explicit correction policy. Recalculate lifecycle signals when late activity changes the historical picture.

## Worked example

Read the example before running it. Identify the input facts or columns, the relationship being expressed, and the expected result.

```python
result = people.dropDuplicates(["name", "company_id"])
```

## A mistake worth recognizing

> A watermark bounds retained state; it does not prove that every late event is included end to end.

Construct a small counterexample that would expose this mistake. Prefer three carefully chosen records to a large dataset whose behavior is hard to explain.

## Predict, then investigate

1. Process a second batch with a duplicate, a late event, and a correction, then reconcile the Gold attributes.
2. Identify what happens when a relevant value or relationship is missing.
3. Change one fact or row and predict which part of the result changes.
4. Explain the result using the vocabulary introduced above.

## Practice

Open the guided lab in this module to inspect its public fixture and starter file. Then solve an independent exercise from an empty implementation. Compare your output with the requirements, including datatypes, identity, and multiplicity where relevant. If you use a hint or reveal a solution, attempt a fresh variant before claiming independent mastery.

## Takeaway

Use event time, watermark, checkpoint, stable event IDs, and an explicit correction policy.

## Official references

- [PySpark DataFrame Guide](https://spark.apache.org/docs/3.5.5/api/python/getting_started/quickstart_df.html)
- [Spark SQL Reference](https://spark.apache.org/docs/3.5.5/sql-ref.html)
- [Spark Performance Tuning](https://spark.apache.org/docs/3.5.5/sql-performance-tuning.html)
- [Databricks medallion architecture](https://docs.databricks.com/gcp/en/lakehouse/medallion)
- [IAB Tech Lab OpenRTB 2.6](https://iabtechlab.com/wp-content/uploads/2022/05/OpenRTB-2.6.pdf)
